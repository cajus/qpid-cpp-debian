--- a/bindings/qpid/Makefile.am
+++ b/bindings/qpid/Makefile.am
@@ -31,8 +31,8 @@
 SUBDIRS += python
 endif
 
-if HAVE_PERL_DEVEL
-SUBDIRS += perl
-endif
+#if HAVE_PERL_DEVEL
+#SUBDIRS += perl
+#endif
 
 endif
